import os
try:
    import flex
except ImportError:
    raise ImportError("""Validator can not find the python package Flex.
    	 You need to install it, to be able to validate the 
    	 swagger api specs. To install it use: pip install flex""")


def main():
	pass


if __name__ == '__main__':
	main()


